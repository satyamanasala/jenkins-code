node
{
  stage('Git Clone'){
     dir ('MAVEN'){
     git 'https://github.com/cit31/studentapp-code.git'
  }
 }
 stage('compile') {
    dir ('MAVEN') {
  sh 'mvn compile'
 }}
 stage('Sonar-cloud'){
  dir ('MAVEN') {
  sh 'mvn compile'
 }
   sh 'mvn sonar:sonar -Dsonar.........'
}
